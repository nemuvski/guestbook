import { css } from '@emotion/react';

const root = css`
  width: 100%;
  margin: 0;
`;

const main = css`
  padding-top: 8rem;
`;

const footer = css`
  margin: 4rem 0;
  text-align: center;
`;

const layoutStyle = {
  root,
  main,
  footer,
};

export default layoutStyle;
