import { css } from '@emotion/react';

const root = css`
  margin-top: 3.5rem;
`;

const loadingText = css`
  margin: 0;
  text-align: center;
`;

const postListStyle = {
  root,
  loadingText,
};

export default postListStyle;
